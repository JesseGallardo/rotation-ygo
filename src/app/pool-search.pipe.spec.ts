import { PoolSearchPipe } from './pool-search.pipe';

describe('PoolSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PoolSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
